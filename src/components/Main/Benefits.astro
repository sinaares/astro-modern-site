---
// src/components/Benefits.astro
import "../../styles/Main/Benefits/StyleSheet.css";

interface Props {
  title?: string;
  description?: string;
  benefits?: Array<{
    title: string;
    description: string;
  }>;
  dashboardStats?: Array<{
    label: string;
    value: number;
  }>;
}

const { title, description, benefits = [], dashboardStats = [] } = Astro.props;
---

<section class="benefits">
  <div class="benefits-container">
    <div class="benefits-content">
      <div class="benefits-text">
        <h2>{title}</h2>
        <p class="lead">{description}</p>

        <div class="benefit-points">
          {
            benefits.map((benefit, index) => (
              <div class="benefit-point">
                <span class="benefit-number">{index + 1}</span>
                <div class="benefit-content">
                  <h4>{benefit.title}</h4>
                  <p>{benefit.description}</p>
                </div>
              </div>
            ))
          }
        </div>
      </div>

      <div class="benefits-visual">
        <div class="visual-card dashboard">
          <div class="card-header">
            <div class="card-dots">
              <span></span>
              <span></span>
              <span></span>
            </div>
            <span class="card-title">Control panel</span>
          </div>
          <div class="card-content">
            <div class="chart-placeholder">
              <div class="dashboard-content">
                <table class="stats-table">
                  {
                    dashboardStats.map((stat) => (
                      <tr>
                        <td>{stat.label}</td>
                        <td class="stat-value">{stat.value}</td>
                      </tr>
                    ))
                  }
                </table>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
